$(document).ready(function() {
  $('#user-<%= @user.id %>').html("<%=j button_to "Unfollow", unfollow_path, :class => "btn btn-primary btn-sm unfollow-button", :method => :post, :remote => true %>")
});
